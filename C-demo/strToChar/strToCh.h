#ifndef STRTOCH_H
#define STRTOCH_H
#include<string>
using std::string;
std::string charToStr(char * s);
std::string charArrToStr(char a[]);
char * strToChar(string s);
const char * strToConstChar(string s);
#endif // STRTOCH_H
